[pytest]
testpaths = ebay_integration/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: requires a live ERPNext instance (deselect with -m "not integration")
addopts = -v -m "not integration"
